apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: tasks.lambda.example.com
spec:
  group: lambda.example.com
  names:
    categories: []
    kind: Task
    plural: tasks
    shortNames: []
    singular: task
  scope: Namespaced
  versions:
  - additionalPrinterColumns: []
    name: v1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for TaskSpec via `CustomResource`
        properties:
          spec:
            properties:
              env:
                default: []
                description: Environment variables to pass to the container
                items:
                  properties:
                    name:
                      type: string
                    value:
                      type: string
                  required:
                  - name
                  - value
                  type: object
                type: array
              handler:
                default: handler
                description: Handler function name (simulates Lambda handler)
                type: string
              image:
                description: Container image to run
                type: string
              imagePullPolicy:
                default: IfNotPresent
                description: Optional image pull policy
                type: string
              resources:
                default:
                  limits: {}
                  requests: {}
                description: Resource requirements
                properties:
                  limits:
                    default: {}
                    properties:
                      cpu:
                        nullable: true
                        type: string
                      memory:
                        nullable: true
                        type: string
                    type: object
                  requests:
                    default: {}
                    properties:
                      cpu:
                        nullable: true
                        type: string
                      memory:
                        nullable: true
                        type: string
                    type: object
                type: object
              timeout:
                default: 300
                description: Timeout in seconds
                format: int64
                type: integer
            required:
            - image
            type: object
          status:
            nullable: true
            properties:
              executions:
                format: int64
                type: integer
              lastExecution:
                nullable: true
                type: string
            required:
            - executions
            type: object
        required:
        - spec
        title: Task
        type: object
    served: true
    storage: true
    subresources:
      status: {}
